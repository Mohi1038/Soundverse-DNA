export interface Creator {
  id?: number;
  name: string;
  image_url?: string;
  genre?: string;
  description?: string;
  audio_preview?: string;
  dnaVisibility: string;
  price: string;
  license: string;
  tracks: string;
  partner: string;
}
